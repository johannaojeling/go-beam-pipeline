source:
  format: "DATABASE"
  database:
    driver: "postgres"
    dsn:
      secret_name: "projects/{{ .Project }}/secrets/cloud-sql-dsn/versions/latest"
    table: "events"
sink:
  format: "BIGQUERY"
  bigquery:
    project: "{{ .Project }}"
    dataset: "raw"
    table: "events"
